import { NavigationKey } from '../types/enums';
import { CellCoordinate } from '../types';
export declare const useKeyboardNavigation: () => (key: NavigationKey, currentRow: number, currentCol: number, maxRow: number, maxCol: number) => CellCoordinate | null;
//# sourceMappingURL=useKeyboardNavigation.d.ts.map